// Tailwind v4 is CSS-first: no tailwind.config.js. The theme lives in the
// `@theme` block in src/app/globals.css.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;

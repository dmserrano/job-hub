# Copy to .env and adjust as needed. Matches docker-compose.yml defaults.
DATABASE_URL=postgres://jobhub:jobhub@localhost:5432/jobhub

# Schema the tracker service owns (ADR-0003). Integration tests override this
# with tracker_test via the test harness.
TRACKER_DB_SCHEMA=tracker

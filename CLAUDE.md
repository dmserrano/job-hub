# job-hub

A personal tool to support the job-search process — studying for interviews, tracking applications, searching for new roles, and related workflows.

## Agent skills

### Issue tracker

Issues and specs live as **GitHub issues**, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`. _(A GitHub remote is not yet configured — push this repo to GitHub before the `gh` workflows will work.)_

### Domain docs

**Single-context** — one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
